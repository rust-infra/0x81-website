node_modules
dist
.git
*.swp
*.log
# 本地开发的 Vite 变量（VITE_API_URL 等）绝不能被 COPY . . 带进构建上下文：
# vite build 会把它们固化进线上产物（本地调试地址会原样打进镜像）。
# 需要给镜像注入 VITE_* 时用构建参数，不要靠目录里的 .env。
# 注意：不带斜杠的模式只匹配根目录那一层，所以两层都写上（子目录里的 .env 同样会被打包）。
.env*
**/.env*
